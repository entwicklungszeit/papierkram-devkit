# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build papierkram-toggl-connector`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t papierkram-toggl-connector`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system papierkram-toggl-connector && \
          adduser --system -G papierkram-toggl-connector papierkram-toggl-connector

COPY dist/apps/papierkram-toggl-connector papierkram-toggl-connector/
RUN chown -R papierkram-toggl-connector:papierkram-toggl-connector .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix papierkram-toggl-connector --omit=dev -f install

CMD [ "node", "papierkram-toggl-connector" ]
